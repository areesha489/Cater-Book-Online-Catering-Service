@{
    var action = ViewContext.RouteData.Values["action"]?.ToString() ?? "";
    var publicPages = new HashSet<string> { "Search", "Restaurants", "CatererDetails" };
    Layout = publicPages.Contains(action) ? "_Layout" : "_CustomerLayout";
}
